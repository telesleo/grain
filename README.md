# grain

**grain** is a lightweight Entity Component System (ECS) library for Rust.

The library is in early development and provides only the core features. Performance optimizations have not yet been applied.
